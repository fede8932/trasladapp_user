import { StyleSheet } from "react-native";

export const setDestinoStyles = StyleSheet.create({
  container: {
    marginBottom: 20,
    padding: 5,
    paddingTop: 10,
    flexDirection: "row",
    alignItems: "center",
  },
});